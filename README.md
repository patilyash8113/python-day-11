# python-day-11
today i learned about strings
(1)learned about indexing
 - learn how indexing work
 - how strings alphabets are count
 - how negative indexing are do
(2) learned about string slicing

(3) string_methods
  - learns len() method that help in counting of strings
  - learns "strips" that help in whitespace
     -there are several types in strip
       (1) lstrip
       (2) rstrip
(4) finding and replacing
This will hep in find a string word and replace sit with other word
 -Find()
 -replace()

(5) spit and join method of string
-This help in convert string to list using (",")
-and join help convert list to string using join statement

(6) text="python123"
print(text.isalpha()) #output:false
print(text.isdigit()) #output:false
print(text.isalnum()) #output:true
print(text.isspace()) #output:false

"This method is help to find a strings true or false about string like
alphabetcal string pr alpha numerical string...



**string formatting and f string**

template="Dear {}, you are awesome.Take this {} bag"

a="john"
a1=10000
b="yash"
b1=1000
c="diksha"
c1=300

s1=template .format(a,a1)
print(s1)

In here we use template format method,
we just enter variables in curly bracket and create print print statement 

***f string***
print(f"Dear {a} you are awesome.Take this {a1} bag")
print(f"Dear {b} you are awesome.Take this {b1} bag")
print(f"Dear {c} you are awesome.Take this {c1} bag")

In f string string formatting become more easy
we just enter the variables name in curly bracket and it automatically execute in print statement..








   
